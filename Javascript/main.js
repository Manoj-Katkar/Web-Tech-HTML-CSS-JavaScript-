// !we will learn how to transfer the function from one file to another
//!we can use the export default keyword to all just we have to use it in the next line first declare it then in the next line use the default keyword 


// create function , object and one string value 

export   function grettings(){
    console.log("Hello Good Morning I am from main");
};

// grettings();

export let obj1 = {
    name : "Manoj",
    course : "Development"
};


let arr1 = [10 , 20 , 30 , 40 ,50];
export default  arr1;     //
export let branch = "Thane 1 min from station I am from main";

// export default let num1 = 100;
